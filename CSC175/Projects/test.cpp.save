#include <iostream>
using namespace std;

int main() {

	int i = 2;
	float a = 0.9;
	int c = i/a;

de:PRIME NUMBER
	/*

	for (int b = 0; b < 200; b++) {
		bool is_prime = true;

		for (int i = b-1; i > 1; i--) {
			if (b % i == 0) {
				is_prime = false;
			}

		}

		if (is_prime) {
			cout << b << " Is prime" << endl;
		}
	}
	*/

	return 0;

}
